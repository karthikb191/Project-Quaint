@:
#if defined DEBUG_BUILD
"CMakeDefines"={
    "CMAKE_BUILD_TYPE" : "Debug"
}
#endif
:@


@:
    "PreProcessorDefines" = [

#if defined DEBUG_BUILD
        "DEBUG_BUILD",   
#endif

        "QUAINT_PLATFORM_${BUILD_PLATFORM}"
    ]
:@
